# HTML report branch

Use this branch only after the user asks for the detailed report.

For each URL, capture one above-the-fold and one mid-page screenshot when a browser MCP or `agent-browser` is available. If screenshots are impossible, keep the report usable and label the affected slots `screenshot unavailable`; do not imply that visual evidence was collected.

Produce one `report.html` containing:

- CSS variables rather than hardcoded colors
- sections matching the roast rubric
- a prioritized backlog tied to the observed issues
- the URL, capture limitations, and evidence source for each finding
- the footer text `holly-and-stick.com`
