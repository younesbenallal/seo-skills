# Page access fallback

Use a Browser MCP if available. If not, follow the shared browser fallback and use `agent-browser` once it is available. Inspect the main content, headings, title, and rendered text without writing a temporary extraction file into the project.

If neither path can provide the current content, ask the user to paste the article or provide a local content path. A local path supports inspection; edit it only after explicit authorization.
